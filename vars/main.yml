---
env_profile: "{{ lookup('env','ANSIBLE_SETUP_PROFILE') }}"
ansible_become_pass: "{{ lookup('env','ANSIBLE_BECOME_PASS') }}"
# Instruct ansible to use python from the pyenv enabled via the setup.sh script
